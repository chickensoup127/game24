using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Item_heart : Entity
{
    // Start is called before the first frame update
    void Start()
    {
    
        tag = "Item_heart";
        InitBattleValue(1, 1);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
